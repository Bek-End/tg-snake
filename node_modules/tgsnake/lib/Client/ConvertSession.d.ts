import { StringSession } from 'telegram/sessions';
import { SnakeSession } from './SnakeSession';
import { Snake } from './Snake';
export declare function ConvertString(session: string, sessionName: string, snakeClient: Snake): Promise<SnakeSession | StringSession>;
export declare function ConvertStore(sessionName: string, snakeClient: Snake): Promise<StringSession>;
