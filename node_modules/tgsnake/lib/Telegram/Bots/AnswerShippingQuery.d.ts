import { Snake } from '../../Client';
export interface ShippingOptions {
    id: string;
    title: string;
    prices: Array<{
        amount: bigint;
        label: string;
    }>;
}
export declare function AnswerShippingQuery(snakeClient: Snake, id: bigint, options?: Array<ShippingOptions>, error?: string): Promise<boolean>;
