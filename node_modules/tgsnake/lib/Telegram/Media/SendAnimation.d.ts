/// <reference types="node" />
import { Snake } from '../../Client';
import { sendMediaMoreParams } from './SendMedia';
import * as Medias from '../../Utils/Medias';
import { onProgress } from './UploadFile';
export interface sendAnimationMoreParams extends sendMediaMoreParams {
    workers?: number;
    mimeType?: string;
    onProgress?: onProgress;
    download?: boolean;
    thumbnail?: Buffer | string;
    width?: number;
    height?: number;
    duration?: number;
    supportsStreaming?: boolean;
}
/**
 * Sending Animation
 * @param snakeClient - Client
 * @param {string|number|bigint} chatId - Chat/Groups/Channel id.
 * @param {string|Buffer} fileId - File Location/Url/Buffer .
 * @param {Object} more - more parameters to use.
 * ```ts
 * bot.command("ani",async (ctx) => {
 *     let results = await ctx.telegram.sendAnimation(ctx.chat.id,"file id here")
 * })
 * ```
 */
export declare function SendAnimation(snakeClient: Snake, chatId: number | string | bigint, fileId: string | Buffer | Medias.MediaAnimation, more?: sendAnimationMoreParams): Promise<import("../../Update").UpdateShortSentMessage | import("../../Update").UpdateNewMessage | import("../../Update").UpdateNewChannelMessage | undefined>;
