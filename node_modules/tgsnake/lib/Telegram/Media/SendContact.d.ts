import { Snake } from '../../Client';
import { defaultSendMediaMoreParams } from './SendMedia';
import * as Medias from '../../Utils/Medias';
export interface InterfaceContact {
    firstName: string;
    lastName: string;
    phoneNumber: string;
    vcard: string;
}
/**
 * Sending Contact
 * @param snakeClient - Client
 * @param {string|number|bigint} chatId - Chat/Groups/Channel id.
 * @param {Object} contact - contact.
 * @param {Object} more - more parameters to use.
 * ```ts
 * bot.command("contact",async (ctx) => {
 *     let results = await ctx.telegram.sendContact(ctx.chat.id,{
 *    firstName : "someone",
 *    lastName : "",
 *    phoneNumber : "1234567890",
 *    vcard : "something info"
 *  })
 * })
 * ```
 */
export declare function SendContact(snakeClient: Snake, chatId: number | string | bigint, contact: InterfaceContact | Medias.MediaContact, more?: defaultSendMediaMoreParams): Promise<import("../../Update").UpdateShortSentMessage | import("../../Update").UpdateNewMessage | import("../../Update").UpdateNewChannelMessage | undefined>;
