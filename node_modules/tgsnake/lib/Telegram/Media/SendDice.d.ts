import { Snake } from '../../Client';
import { defaultSendMediaMoreParams } from './SendMedia';
import * as Medias from '../../Utils/Medias';
/**
 * Sending Dice
 * @param snakeClient - Client
 * @param {string|number|bigint} chatId - Chat/Groups/Channel id.
 * @param {string|Object} dice - dice emoticon .
 * @param {Object} more - more parameters to use.
 * ```ts
 * bot.command("dice",async (ctx) => {
 *     let results = await ctx.telegram.sendDice(ctx.chat.id,"🎯")
 * })
 * ```
 */
export declare function SendDice(snakeClient: Snake, chatId: number | string | bigint, dice: string | Medias.MediaDice, more?: defaultSendMediaMoreParams): Promise<import("../../Update").UpdateShortSentMessage | import("../../Update").UpdateNewMessage | import("../../Update").UpdateNewChannelMessage | undefined>;
