"use strict";
// Tgsnake - Telegram MTProto framework developed based on gram.js.
// Copyright (C) 2022 Butthx <https://github.com/butthx>
//
// This file is part of Tgsnake
//
// Tgsnake is a free software : you can redistribute it and/or modify
//  it under the terms of the MIT License as published.
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.SendDice = void 0;
const telegram_1 = require("telegram");
const SendMedia_1 = require("./SendMedia");
const Error_1 = __importDefault(require("../../Context/Error"));
/**
 * Sending Dice
 * @param snakeClient - Client
 * @param {string|number|bigint} chatId - Chat/Groups/Channel id.
 * @param {string|Object} dice - dice emoticon .
 * @param {Object} more - more parameters to use.
 * ```ts
 * bot.command("dice",async (ctx) => {
 *     let results = await ctx.telegram.sendDice(ctx.chat.id,"🎯")
 * })
 * ```
 */
async function SendDice(snakeClient, chatId, dice, more) {
    try {
        snakeClient.log.debug('Running telegram.sendDice');
        if (typeof chatId === 'number')
            snakeClient.log.warning('Type of chatId is number, please switch to BigInt or String for security Ids 64 bit int.');
        //@ts-ignore
        let emoji = typeof dice == 'string' ? dice : dice.emoji;
        return await (0, SendMedia_1.SendMedia)(snakeClient, chatId, new telegram_1.Api.InputMediaDice({
            emoticon: emoji,
        }), more);
    }
    catch (error) {
        snakeClient.log.error('Failed to running telegram.sendDice');
        throw new Error_1.default(error.message, 'telegram.sendDice', `${chatId},${dice}${more ? ',' + JSON.stringify(more) : ''}`);
    }
}
exports.SendDice = SendDice;
