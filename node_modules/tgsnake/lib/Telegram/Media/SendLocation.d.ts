import { Snake } from '../../Client';
import { defaultSendMediaMoreParams } from './SendMedia';
import * as Medias from '../../Utils/Medias';
export interface InterfaceLocation {
    latitude: number;
    longitude: number;
    accuracyRadius?: number;
}
/**
 * Sending Location
 * @param snakeClient - Client
 * @param {string|number|bigint} chatId - Chat/Groups/Channel id.
 * @param {Object} location - location
 * @param {Object} more - more parameters to use.
 * ```ts
 * bot.command("loc",async (ctx) => {
 *     let results = await ctx.telegram.sendLocation(ctx.chat.id,{
 *    latitude : 0,
 *    longitude : 0
 *  })
 * })
 * ```
 */
export declare function SendLocation(snakeClient: Snake, chatId: number | string | bigint, location: InterfaceLocation | Medias.MediaLocation, more?: defaultSendMediaMoreParams): Promise<import("../../Update").UpdateShortSentMessage | import("../../Update").UpdateNewMessage | import("../../Update").UpdateNewChannelMessage | undefined>;
