import { Api } from 'telegram';
import { Snake } from '../../Client';
import { TypeReplyMarkup } from '../../Utils/ReplyMarkup';
import { Entities } from '@tgsnake/parser';
import * as Update from '../../Update';
export interface defaultSendMediaMoreParams {
    silent?: boolean;
    background?: boolean;
    clearDraft?: boolean;
    replyToMsgId?: number;
    scheduleDate?: number;
    noforwards?: boolean;
    sendAs?: string;
    replyMarkup?: TypeReplyMarkup;
}
export interface sendMediaMoreParams extends defaultSendMediaMoreParams {
    entities?: Entities[];
    parseMode?: string;
    caption?: string;
}
/**
 * Sending message media.
 * @param snakeClient - Client
 * @param {number|string|bigint} chatId - Chat/Groups/Channel id.
 * @param {Object} media - Message Media.
 * @param more - more parameters to use.
 */
export declare function SendMedia(snakeClient: Snake, chatId: number | string | bigint, media: Api.TypeInputMedia, more?: sendMediaMoreParams): Promise<Update.UpdateShortSentMessage | Update.UpdateNewMessage | Update.UpdateNewChannelMessage | undefined>;
