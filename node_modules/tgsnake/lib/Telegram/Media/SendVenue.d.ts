import { Snake } from '../../Client';
import { defaultSendMediaMoreParams } from './SendMedia';
import * as Medias from '../../Utils/Medias';
export interface InterfaceVenue {
    latitude: number;
    longitude: number;
    accuracyRadius?: number;
    title: string;
    address: string;
    provider: string;
    id: string;
    type: string;
}
/**
 * Sending Venue
 * @param snakeClient - Client
 * @param {string|number|bigint} chatId - Chat/Groups/Channel id.
 * @param {Object} venue - venue
 * @param {Object} more - more parameters to use.
 * ```ts
 * bot.command("venue",async (ctx) => {
 *     let results = await ctx.telegram.sendVenue(ctx.chat.id,{
 *    latitude : 0,
 *    longitude : 0,
 *    title : "title",
 *    address : "address",
 *    provider : "provider",
 *    id : "some id here",
 *    type : "some type here"
 *  })
 * })
 * ```
 */
export declare function SendVenue(snakeClient: Snake, chatId: number | string | bigint, venue: InterfaceVenue | Medias.MediaVenue, more?: defaultSendMediaMoreParams): Promise<import("../../Update").UpdateShortSentMessage | import("../../Update").UpdateNewMessage | import("../../Update").UpdateNewChannelMessage | undefined>;
