/// <reference types="node" />
import { Snake } from '../../Client';
import { sendMediaMoreParams } from './SendMedia';
import * as Medias from '../../Utils/Medias';
import { onProgress } from './UploadFile';
export interface sendVideoNoteMoreParams extends sendMediaMoreParams {
    workers?: number;
    mimeType?: string;
    onProgress?: onProgress;
    download?: boolean;
    thumbnail?: Buffer | string;
    width?: number;
    height?: number;
    duration?: number;
    supportsStreaming?: boolean;
}
/**
 * Sending VideoNote (rounded video)
 * @param snakeClient - Client
 * @param {string|number|bigint} chatId - Chat/Groups/Channel id.
 * @param {string|Buffer} fileId - File Location/Url/Buffer .
 * @param {Object} more - more parameters to use.
 * ```ts
 * bot.command("vidnote",async (ctx) => {
 *     let results = await ctx.telegram.sendVideoNote(ctx.chat.id,"file id here")
 * })
 * ```
 */
export declare function SendVideoNote(snakeClient: Snake, chatId: number | string | bigint, fileId: string | Buffer | Medias.MediaVideoNote, more?: sendVideoNoteMoreParams): Promise<import("../../Update").UpdateShortSentMessage | import("../../Update").UpdateNewMessage | import("../../Update").UpdateNewChannelMessage | undefined>;
