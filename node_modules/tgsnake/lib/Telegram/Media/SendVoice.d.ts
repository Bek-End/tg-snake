/// <reference types="node" />
import { Snake } from '../../Client';
import { sendMediaMoreParams } from './SendMedia';
import * as Medias from '../../Utils/Medias';
import { onProgress } from './UploadFile';
export interface sendVoiceMoreParams extends sendMediaMoreParams {
    workers?: number;
    mimeType?: string;
    onProgress?: onProgress;
    download?: boolean;
    thumbnail?: Buffer | string;
    duration?: number;
    title?: string;
    performer?: string;
    filename?: string;
}
/**
 * Sending Voice
 * @param snakeClient - Client
 * @param {string|number|bigint} chatId - Chat/Groups/Channel id.
 * @param {string|Buffer} fileId - File Location/Url/Buffer .
 * @param {Object} more - more parameters to use.
 * ```ts
 * bot.command("voice",async (ctx) => {
 *     let results = await ctx.telegram.sendVoice(ctx.chat.id,"file id here")
 * })
 * ```
 */
export declare function SendVoice(snakeClient: Snake, chatId: number | string | bigint, fileId: string | Buffer | Medias.MediaVoice, more?: sendVoiceMoreParams): Promise<import("../../Update").UpdateShortSentMessage | import("../../Update").UpdateNewMessage | import("../../Update").UpdateNewChannelMessage | undefined>;
