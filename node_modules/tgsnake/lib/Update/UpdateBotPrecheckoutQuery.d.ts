import { Api } from 'telegram';
import { From } from '../Utils/From';
import { Update } from './Update';
import { Snake } from '../Client';
import { PaymentRequestedInfo } from '../Utils/Payment';
export declare class UpdateBotPrecheckoutQuery extends Update {
    id: bigint;
    from: From;
    payload: string;
    info?: PaymentRequestedInfo;
    shippingOptionId?: string;
    currency: string;
    totalAmount: bigint;
    constructor();
    init(update: Api.UpdateBotPrecheckoutQuery, SnakeClient: Snake): Promise<this>;
    answer(ok: boolean, error?: string): Promise<boolean>;
}
