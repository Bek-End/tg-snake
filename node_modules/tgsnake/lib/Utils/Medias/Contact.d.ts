import { Api } from 'telegram';
import { Media } from './Media';
import { Snake } from '../../Client';
export declare class MediaContact extends Media {
    phoneNumber: string;
    firstName: string;
    lastName?: string;
    vcard: string;
    userId: bigint;
    constructor();
    encode(contact: Api.MessageMediaContact, snakeClient: Snake): this;
}
