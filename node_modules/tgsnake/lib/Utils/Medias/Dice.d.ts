import { Api } from 'telegram';
import { Media } from './Media';
import { Snake } from '../../Client';
export declare class MediaDice extends Media {
    emoji: string;
    value: number;
    constructor();
    encode(dice: Api.MessageMediaDice, snakeClient: Snake): Promise<this>;
}
