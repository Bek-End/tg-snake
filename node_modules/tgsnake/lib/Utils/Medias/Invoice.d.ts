import { Api } from 'telegram';
import { Media } from './Media';
import { Snake } from '../../Client';
export declare class MediaInvoice extends Media {
    /** @hidden */
    _photo?: Api.TypeWebDocument;
    shippingAddressRequested?: boolean;
    test?: boolean;
    receiptMsgId?: number;
    title: string;
    description: string;
    currency: string;
    totalAmount: bigint;
    startParam: string;
    constructor();
    encode(invoice: Api.MessageMediaInvoice, snakeClient: Snake): Promise<this>;
}
