import { Api } from 'telegram';
import { Media } from './Media';
import { Snake } from '../../Client';
export declare class MediaLocation extends Media {
    latitude: number;
    longitude: number;
    accessHash: bigint;
    accuracyRadius: number;
    constructor();
    encode(location: Api.MessageMediaGeo | Api.TypeGeoPoint, snakeClient: Snake): Promise<this>;
}
