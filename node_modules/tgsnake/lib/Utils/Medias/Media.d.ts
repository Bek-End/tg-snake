/// <reference types="node" />
import { Snake } from '../../Client';
import { inspect } from 'util';
export type TypeMessageMediaName = 'sticker' | 'document' | 'photo' | 'location' | 'dice' | 'contact' | 'animation' | 'video' | 'poll' | 'venue' | 'videoNote' | 'voice' | 'audio' | 'webPage' | 'game' | 'invoice' | 'chatPhoto';
export declare class Media {
    _: TypeMessageMediaName;
    date: Date | number;
    /** @hidden */
    private _SnakeClient;
    constructor();
    get snakeClient(): Snake;
    get SnakeClient(): Snake;
    set snakeClient(client: Snake);
    set SnakeClient(client: Snake);
    decode(fileId?: string): import("tg-file-id/dist/types/FileIdInfo").FileIdInfo;
    /** @hidden */
    [inspect.custom](): {
        [key: string]: any;
    };
    /** @hidden */
    toJSON(): {
        [key: string]: any;
    };
}
