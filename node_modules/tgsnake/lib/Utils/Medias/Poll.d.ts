import { Api } from 'telegram';
import { Media } from './Media';
import { Snake } from '../../Client';
import { Entities } from '@tgsnake/parser';
export interface IPollAnswer {
    text: string;
    chosen: boolean;
    correct: boolean;
    voters: number;
    option: string;
}
export declare class MediaPoll extends Media {
    id: bigint;
    closed: boolean;
    publicVoters: boolean;
    multipleChoice: boolean;
    quiz: boolean;
    question: string;
    answers: Array<IPollAnswer>;
    closePeriod: number;
    closeDate: number;
    min: boolean;
    totalVoters: number;
    recentVoters: any;
    solution?: string;
    solutionEntities?: Array<Entities>;
    constructor();
    encode(poll: Api.MessageMediaPoll, snakeClient: Snake): Promise<this>;
}
