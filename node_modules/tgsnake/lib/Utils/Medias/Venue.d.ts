import { Api } from 'telegram';
import { Media } from './Media';
import { Snake } from '../../Client';
export declare class MediaVenue extends Media {
    latitude: number;
    longitude: number;
    accessHash: bigint;
    accuracyRadius: number;
    title: string;
    address: string;
    provider: string;
    id: string;
    type: string;
    constructor();
    encode(venue: Api.MessageMediaVenue, snakeClient: Snake): Promise<this>;
}
